<!DOCTYPE html>
<html>
<head>
  <title>Password Forget</title>
</head>
<body>
<%- include('../partials/header') %>

<section class="sectionmin">
  <h1  class="mainheading">Password Forget</h1>
  <p class="maintext">
    Please enter your registered email to receive a One Time Password (OTP) to reset your password.
  </p>
  <form class="formpart" id="formpart" action="/users/password-forget" method="post">
    <% if (error && typeof error === 'string') { %>
      <aside class="errormessage"><%= error %></aside>
    <% } %>
    <% if (success) { %>
      <aside class="successmessage"><%= success %></aside>
      <script>
        setTimeout(() => {
          window.location.href = '/users/password-otp';
        }, 2000); 
      </script>
    <% } %>
    
  
  <div>
    <label>Enter Your Email ID</label>
    <input type="text" name="email" value="<%= email || '' %>" />
    <% if (error && error.email) { %>
      <span class="error"><%= error.email %></span>
    <% } %>
  </div>

  <%- include('../partials/button', { text: 'Submit' }) %>

  <div class="formbottom">
      <p>Back to <a href="/users/login">Login</a></p> 
      <p>Not have account <a href="/users/register">Create Account</a></p>
  </div>
</form>

</section>
<%- include('../partials/footer', { title: 'EMP Management System' }) %>

 
</body>
</html>
